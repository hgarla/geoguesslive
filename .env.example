# OpenAI API key for gpt-image-1 generation
OPENAI_API_KEY=sk-...

# Optional: shared secret to authenticate Vercel Cron calls to /api/cron/generate.
# Vercel auto-injects this on cron invocations when set as a project env var.
CRON_SECRET=
